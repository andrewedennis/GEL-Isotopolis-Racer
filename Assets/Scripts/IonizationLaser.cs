using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IonizationLaser : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        Destroy(this, 1f);

    }

    // Update is called once per frame
    void Update()
    {


        
    }

    
}
